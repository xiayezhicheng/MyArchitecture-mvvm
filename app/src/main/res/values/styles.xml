<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--<item name="android:navigationBarColor">@color/color_primary</item>-->
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ImageTranslucentTheme" parent="AppTheme">
        <!--在Android 4.4之前的版本上运行，直接跟随系统主题-->
    </style>

    <style name="ColorTranslucentTheme" parent="AppTheme">

    </style>

    <style name="Theme.Translucent.Navigation" parent="AppTheme">

    </style>

    <style name="draweeItem">
        <item name="actualImageScaleType">centerCrop</item>
        <item name="placeholderImage">@drawable/img_placeholder</item>
        <item name="failureImage">@drawable/img_failure</item>
    </style>

</resources>
